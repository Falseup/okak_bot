## ОКАК бот (aiogram)

Телеграм-бот для групповых чатов, который с вероятностью 15% отвечает
на любое сообщение фразой «окак». Пользователи из отдельного списка
получают гарантированные кастомные ответы, а игнор-лист полностью
отключает реакции.

### Настройка
- Установите зависимости: `pip install -r requirements.txt`
- Создайте бота через [@BotFather](https://t.me/BotFather)
- Откройте `config.py` и впишите токен в переменную `BOT_TOKEN`
- Заполните файл в каталоге `data/`
  - `special_phrases.txt` — фразы для особых пользователей

### Запуск
```
python main.py
```

Файлы в `data/` перечитываются при каждом сообщении, поэтому менять их
можно без перезапуска.